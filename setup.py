#!/usr/bin/env python
from setuptools import setup, find_packages

setup(
    name = 'pyActiveSync',
    version = '0.2',
    description = 'pyActiveSync is a pure python client-side library for the Microsoft ActiveSync protocol.',
    author = 'Mathieu Rodic',
    author_email = 'mathieu@iko-system.com',
    packages = find_packages(),
    license = 'GNU GENERAL PUBLIC LICENSE',
    install_requires = [],
    zip_safe = True,
)
